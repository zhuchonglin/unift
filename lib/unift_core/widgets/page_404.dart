import 'package:flutter/material.dart';

class PageNotFind extends StatelessWidget {
  const PageNotFind({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: const BoxDecoration(color: Colors.white),
    );
  }
}
